{
  "name": "parse-server-sendgrid",
  "version": "1.0.0",
  "description": "Email adapter for Parse Server that uses SendGrid.",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Erikas Taroza",
  "license": "MIT",
  "dependencies": {
    "@sendgrid/mail": "^7.7.0"
  }
}
